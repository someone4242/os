#include <stdtab.h>
// #include <stdlib.h>

tab_t *tab_create() {
    /*
    char *data = (char *)malloc(DISPLAY_SIZE * sizeof(char));
    tab_t *tab = (tab_t *)malloc(sizeof(tab_t));

    tab->width = DISPLAY_WIDTH;
    tab->height = DISPLAY_HEIGHT;
    tab->size = DISPLAY_SIZE;
    tab->cursor = 0;
    tab->autoscroll = false;
    tab->data = data;

    for (size_t i; i < DISPLAY_SIZE; i++) {
        tab->data[i] = ' ';
    }

    return tab;
    */

    return NULL;
}
